# Vue3新内容学习

### 1.ref vs reactive
| 类型 | 支持数据 | 读写方式 | 重新赋值 | 解构/传参 |
|------|----------|----------|----------|------------|
| ref | 任意（含原始值） | `.value` | 安全 | 安全 |
| reactive | 仅对象/数组 | 直接 `.属性` | ❌ 丢失响应 | ❌ 丢失响应 |

**使用建议**：优先用 `ref`，除非是很大的对象，并且不用从新赋值和传参

**示例**：
```js
const count = ref(0)
count.value++

const obj = reactive({ a:1 })
obj.a++
// obj = { a:2 } 错误