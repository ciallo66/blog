# Vue3新内容学习

## 1.ref vs reactive
| 类型 | 支持数据 | 读写方式 | 重新赋值 | 解构/传参 |
|------|----------|----------|----------|------------|
| ref | 基本数据,数组） | `.value` | 安全 | 安全 |
| reactive | 仅对象/数组 | 直接 `.属性` | ❌ 丢失响应 | ❌ 丢失响应 |

**使用建议**：优先用 `ref`，除非是很大的对象，并且不用从新赋值和传参

**示例**：
```js
const count = ref(0)
count.value++

const obj = reactive({ a:1 })
obj.a++
// obj = { a:2 } 错误
```

## computer
- **缓存**：依赖不变，多次访问不重新计算。
- **懒执行**：依赖变化且被读取时才重算。
- **返回值**：只读的 ref 对象。
- **可写**：用 `{ get, set }` 形式。

```js
// 只读
const double = computed(() => count.value * 2)

// 可写
const full = computed({
  get: () => first.value + last.value,
  set: (v) => { /* 拆分赋值 */ }
})
```

## 监视
### watch
- 只能监视一下数据
>1.ref 定义的数据  
>2.reactive 定义的数据  
>3.函数返回的一个值(getter函数)  
>4.一个包含上述内容的数组  

- 特点：
**懒执行**：默认只在侦听源变化时执行，不会立即运行。  
**侦听源**：可以是 ref、reactive 响应式数据、getter 函数，或多个源的数组。  
**深度监听**：监听对象/数组内部变化需开启 `{ deep: true }`,用reactive默认深度监视,且无法关闭。  
**立即执行**：配置 `{ immediate: true }` 让回调初始时立即运行一次。  
**停止监听**：watch 函数返回一个停止函数，调用即可停止。  

```js
// 单个 ref
watch(()=>count, (newVal, oldVal) => {})

// getter 函数
watch(() => state.name, (newVal, oldVal) => {})

// 多个源
watch([()=>count, ()=>state.name], ([newCount, newName], [oldCount, oldName]) => {})

// 深度 + 立即
watch(()=>count, (newVal, oldVal) => {}, { deep: true, immediate: true })
```
### watchEffect
- 特点：可以自动对操作的属性进行监视，不用写监视的属性
``` js
watchEffect(()=>{
    if(a>=6 || b>=4){
        console.log('BIG!')
    }
})
//自动监视a和b
```

### props
- 子组件里面获取
>纯 TS 路（泛型模式）：defineProps<{ 属性: 类型 }>()   
>//它的圆括号里必须是空的！所有规则全靠大括号和尖括号。

>纯 JS 路（运行时模式）：defineProps(['List']) 或者 defineProps({ List: Object })   
>它的尖括号是完全没有的，规则全写在圆括号里。