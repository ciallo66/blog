# 看板娘立绘替换说明

1. 准备一张 **透明背景 PNG**（建议高度 400～600px，竖版立绘）。
2. 命名为 `character.png`，放在本目录（与 `default.svg` 同级）。
3. 刷新页面即可；若图片路径错误或加载失败，会自动显示 `default.svg`。

也可修改 `src/config/mascot.ts` 里的 `image` 字段指向其他文件名，例如 `mascot/my-waifu.png`。
